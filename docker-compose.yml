version: "3.9"
services:
  frontend:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    build: ./server
    ports:
      - "8787:8787"
    env_file:
      - ./server/.env.example
